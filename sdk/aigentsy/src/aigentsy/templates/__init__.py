"""Bundled scaffold templates for `aigentsy create-agent`.

Each subdirectory is one template (e.g. `settlement_native_mcp`). Files inside
each subdirectory are copied into the generated project; files with a
`.template` extension are processed for placeholder substitution
(`{{PROJECT_NAME}}` becomes the user-supplied name) and the `.template`
suffix is stripped.
"""
